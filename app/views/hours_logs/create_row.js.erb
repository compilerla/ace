$('.addrowbutton.clicked').closest('table').find('tr').last().before("<%=j render partial: 'hours_logs/row', locals: { hours_type: 'lunch', week_start: @week_start } %>");
$('.addrowbutton.clicked').removeClass('clicked');